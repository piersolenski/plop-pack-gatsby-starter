import PropTypes from 'prop-types';
{{{svgr code (properCase name)}}}
{{properCase name}}.propTypes = {
  title: PropTypes.string,
  titleId: PropTypes.string,
};

{{properCase name}}.defaultProps = {
  titleId: '{{properCase name}}',
};
